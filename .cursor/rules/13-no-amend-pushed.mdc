---
description: Never amend commits that have been pushed to remote
alwaysApply: true
---

# Never amend pushed commits

## Rule

**NEVER use `git commit --amend` on any commit that exists on a remote branch.**

Amending rewrites the commit SHA. When the old SHA is already on `origin/staging`, local and remote diverge.

## What to do instead

Create a **new commit**:

```bash
git add <path>...
git commit -m "fix(billing)[#312]: adjust plan limit check"
```

## The only exception

`--amend` is safe **only** when ALL of these are true:
1. The commit was created by **this agent** in **this conversation**
2. The commit has **never been pushed** to any remote
3. The user explicitly asked to amend

## Anti-patterns

```bash
# WRONG
git commit --amend --no-edit
git push --force-with-lease

# CORRECT
git commit -m "fix(api)[#312]: fix webhook timing-safe compare"
```
