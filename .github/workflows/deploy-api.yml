name: Deploy API

on:
  workflow_call:
    inputs:
      environment:
        description: GitHub Actions environment (staging | production)
        type: string
        required: true
      app:
        description: Fly.io app name (e.g. pipewatch-staging-api)
        type: string
        required: true
      sentry_release:
        description: Sentry release identifier (pipewatch@version+sha)
        type: string
        required: true

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy API (${{ inputs.app }})
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup workspace
        uses: ./.github/actions/setup
        with:
          skip-checkout: "true"

      - name: Build API
        env:
          NODE_ENV: production
        run: pnpm exec turbo run build --filter=@pipewatch/api...

      - name: Create Sentry release and upload API source maps
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
          SENTRY_PROJECT: pipewatch-api
        run: >-
          bash .github/scripts/sentry-release.sh api apps/api/dist
          "${{ inputs.sentry_release }}" "${{ inputs.environment }}"

      - name: Setup Flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy API to Fly.io
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          FLY_APP: ${{ inputs.app }}
        run: bash .github/scripts/deploy-fly.sh api
