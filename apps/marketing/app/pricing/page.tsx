import type { Metadata } from "next";

import { PricingPage } from "@/components/pricing";

export const metadata: Metadata = {
  title: "Pricing",
  description:
    "Usage-based pricing for PipeWatch Cloud — Free, Pro, and Business plans. No per-seat fees. Self-host CE for free.",
};

export default PricingPage;
