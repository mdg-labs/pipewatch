#!/usr/bin/env node
/**
 * E2E test entrypoint — Playwright wrapper is implemented in a later task.
 */
process.stdout.write("test:e2e stub — Playwright suite not yet configured\n");
process.exit(0);
