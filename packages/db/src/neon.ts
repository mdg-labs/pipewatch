/**
 * Neon serverless driver — re-exported for edge/serverless consumers.
 * Node.js services use `postgres` via `createDb()` / `db` (works with Neon URLs).
 */
export { neon, neonConfig } from "@neondatabase/serverless";
